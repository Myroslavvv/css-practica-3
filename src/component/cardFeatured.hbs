{{#with data}}
    <div class="card">
        <div class="card-image" style="background-image: url({{photo}});"></div>
        <h2 class="card-title">{{heading}}</h2>
        <p class="card-text">{{info}}</p>
        <a href="{{linkUrl}}" class="card-button">{{linkText}}</a>
    </div>
{{/with}}